#include "GlobalModificator.h"

namespace model {
    GlobalModificator::GlobalModificator(int id, std::time_t startDate, std::time_t endDate)
    : mId(id), mStartDate(startDate), mEndDate(endDate)
    {

    }
}